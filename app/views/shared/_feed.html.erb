<% if @feed_items.any? %>
	<ol class="microposts">
		<%= render partial: 'shared/feed_item', collection: @feed_items %>  #partialがないとfeed_items/feed_itemを参照してしまう
	</ol>
	<%= will_paginate @feed_items %>
<% end %> 